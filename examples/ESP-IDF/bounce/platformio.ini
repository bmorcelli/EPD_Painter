[env:esp32s3]
platform = espressif32
board = esp32s3box
framework = espidf
build_flags =
    -DBOARD_HAS_PSRAM
    -mfix-esp32-psram-cache-issue
    -O3

# We use the library itself from the parent directory
lib_extra_dirs = ../../../
